# ★ 記事の編集ガイド（VS Code・自分で直す用）

文章の言い回し・誤字・数値の微修正を、**自分でタダで**直すための手引きです。
（デザインの色やレイアウト・グラフは触らない＝Claudeに頼む）

---

## 0. 基本の流れ（4ステップ）

1. 左の一覧から `articles/○○.html` をクリックして開く
2. `Cmd + F` で直したい文字を検索して見つける
3. 文字を書き換える
4. `Cmd + S` で保存 → プレビューに反映される

---

## 1. プレビューの出し方（保存で即反映）

1. 開いた `.html` ファイルの右上にある **「Show Preview」アイコン**（虫めがね/分割画面みたいなアイコン）をクリック
   - または、編集エリアを右クリック →「Show Preview」
2. 右側に記事の見た目が出ます。`Cmd + S` で保存するたびに更新されます。

---

## 2. 触っていい場所・ダメな場所（最重要）

HTMLは `<タグ>文章</タグ>` という形です。**「> と < にはさまれた文章」だけ触ればOK。**

### ✅ 触っていい（文章そのもの）
```
<p>ここの文章は自由に直してOKです。</p>
              ^^^^^^^^^^^^^^^^^^^^^ ← ここだけ
<h2 id="result">この見出しの文字もOK</h2>
                ^^^^^^^^^^^^^^^^
<td>120pt</td>   ← 表の中の数字もOK
    ^^^^^
```

### ❌ 触らない（壊れる原因）
- `<` と `>` の**記号そのもの**と、その中身（`<p>` `<div class="...">` `id="result"` など）
- `assets/` フォルダの中（`style.css`＝全ページのデザイン、`site.js`＝動く仕掛け。1文字のミスで全ページが崩れます）
- 画像の `<img src="...">` のファイル名部分

> 迷ったら：**日本語の文章だけ**を直す、と覚えればほぼ安全です。

---

## 3. よくある編集の例

| やりたいこと | やり方 |
|---|---|
| 言い回しを変える | `Cmd+F`で元の文を検索 → 文章を書き換え → 保存 |
| 誤字を直す | 同上 |
| 数字を直す | 表の `<td>○○</td>` の○○だけ書き換え |
| タイトルを変える | `<h1>〜</h1>` の中の文字を書き換え |

---

## 4. やってしまったとき（元に戻す）

- 直後なら `Cmd + Z`（取り消し）を連打
- 保存前なら、ファイルを閉じて「保存しない」を選べば元のまま

---

## 5. 公開（本番反映）について

VS Codeで直して保存しても、それは**自分のPCの中だけ**です。
インターネットに公開するには「デプロイ」という別の作業が必要です。
→ ここはClaudeに「ブログをデプロイして」と頼んでください（or 専用スクリプトを用意可）。

---

## 6. 触らない方がいいファイル

- `assets/style.css` と `assets/site.js`（全ページ共通のデザインと動き。ここを触ると全記事に影響）
- `*.html` の `<head>` の中
- `wrangler.jsonc` / `sitemap.xml` / `robots.txt`（設定系）
- `articles/images/` の中（画像）

これらを変えたいときはClaudeへ。
